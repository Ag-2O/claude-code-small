---
description: >
  TDD実装: $ARGUMENTS
  plan_$ARGUMENTS.md を基に、RED → GREEN → REFACTOR サイクルで各ステップを実装する。
---

# 指示

TDD実装: $ARGUMENTS

ステップ名または機能名 `$ARGUMENTS` で `coder` サブエージェントを呼び出す。
実装ワークフローは `flow-tdd` スキルに従うよう指示する。

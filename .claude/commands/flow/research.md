---
description: >-
  調査: $ARGUMENTS
  技術的な疑問を researcher サブエージェントで調査し、research/[topic].md へ記録する。
argument-hint: <topic / question>
---

# 指示

調査: $ARGUMENTS

調査トピック `$ARGUMENTS` で `researcher` サブエージェントを呼び出すこと。
ナレッジベース確認・Web 検索・調査結果（`research/[topic].md`）の記録は `researcher` 定義の責務に従って実行される（ここでは再指示しない）。

---
description: >-
  コードベース探索: $ARGUMENTS
  対象エリアを explorer サブエージェントで分析し、project_architecture.md を更新する。
argument-hint: <target> [purpose]
---

# 指示

コードベース探索: $ARGUMENTS

調査対象（機能名・エリア名・課題名）`$ARGUMENTS` で `explorer` サブエージェントを呼び出すこと。
コードベースの分析と `.artifacts/project_architecture.md` の差分更新は `explorer` 定義の責務に従って実行される（ここでは再指示しない）。
